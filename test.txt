Audit current runtime, CLI, config, and dev-server to scope feature additions

Design suspense/streaming runtime additions and CLI workflow improvements

Implement runtime enhancements, CLI updates, and config presets

Enhance dev-server tooling and integrate streaming support

Validate changes and outline follow-up documentation updates